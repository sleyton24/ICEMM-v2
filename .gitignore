# Lock/temporales del entorno
.849*
*.tmp
*~

# Sistema operativo
Thumbs.db
Desktop.ini
.DS_Store

# Dependencias futuras (cuando se migre a Vite)
node_modules/
dist/

# Config local de Claude (no productivo)
.claude/settings.local.json

# Módulo Control de Costos (app separada: Python + backend Node/Prisma + frontend Vite).
# IGNORADO POR SEGURIDAD: contiene credenciales SQL en texto plano (informe_lq.py,
# exportar_*.py), un .git anidado y una copia duplicada icemm~/. No commitear hasta
# decidir cómo versionarlo (rotar clave, mover a .env, quitar .git anidado + icemm~/).
ICEMM/

# Ejecutable del launcher (se regenera desde "Abrir App ICEMM.ps1" con ps2exe).
*.exe

# Config local de Claude con rutas específicas de la máquina (no compartir).
.claude/settings.json
